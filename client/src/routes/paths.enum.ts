export enum paths {
    home = '/',
    customer = '/customers',
    billsHome = '/bills'
}

export enum billsPaths {
    home = '/',
    billDetail = '/open/:id'
}

export enum customersPaths {
    home = '/'
}